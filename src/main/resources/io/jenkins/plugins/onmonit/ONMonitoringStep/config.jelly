<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">

    <f:entry field="port">
        <f:textbox title="${%Node exporter listen on port}" />
    </f:entry>

    <f:entry field="debug">
        <f:checkbox title="${%Log output}" default="false" />
    </f:entry>

    <f:entry field="neAdditionalOptions">
        <f:textbox title="${%Otel-contrib additional options}" />
    </f:entry>

    <f:entry field="ocAdditionalOptions">
        <f:textbox title="${%Otel-contrib additional options}" />
    </f:entry>

</j:jelly>
