<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">

    <f:entry field="port" title="${%Node exporter listen on port}">
        <f:textbox />
    </f:entry>

    <f:entry field="debug" title="${%Log output}">
        <f:checkbox default="false" />
    </f:entry>

    <f:entry field="neAdditionalOptions" title="${%Node_exporter additional options}">
        <f:textbox />
    </f:entry>

    <f:entry field="ocAdditionalOptions" title="${%Otel-contrib additional options}">
        <f:textbox />
    </f:entry>

</j:jelly>
